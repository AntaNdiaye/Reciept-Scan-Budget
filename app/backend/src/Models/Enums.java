package Models;

public interface Enums {

    public enum Categories {
        FOOD, CLOTHING, EDUCATION, TRAVEL, ENTERTAINMENT, MEDICAL, HOME
    }

    public enum Notification {
        OVER_BUDGET, REPEAT_PURCHASE, WITHIN_BUDGET,
    }
}
